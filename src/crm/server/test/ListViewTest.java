package crm.server.test;

import junit.framework.TestCase;

public class ListViewTest extends TestCase {
	// TODO have to rewrite this testcase
	public void testColumnConsistency() {
		/*	final ListView lv = new ListView(DtoContact.class);

		delayTestFinish(500);

		Timer loadTimer = new Timer() {
			@Override
			public void run() {
				assert(0 == lv.currentPage());
				finishTest();
			}
		};
		loadTimer.schedule(250);
	*/}
/*
	@Override
	public String getModuleName() {
		return "crm.Gae";
	}*/
}
